---
name: kodama-review
description: kodama で人間が書いたコードレビューの指摘を読み、対応して返信・解決する。ユーザーが「レビュー見て」「指摘に対応して」「kodama の指摘」と言ったとき、または diff レビューの結果を反映する作業のときに使う。
---

# kodama のレビューに対応する

人間が kodama（GUI）で diff に指摘を書き、それを CLI から読んで直す。

## 手順

### 1. 指摘を読む

作業対象の worktree で実行する。

```bash
kodama review list
```

未解決の指摘が Markdown で出る。0 件なら `未解決の指摘はありません。` の 1 行。

別の worktree を見るときは `--worktree <DIR>`、解決済みも見るときは
`--status all`、構造化して扱いたいときは `--format json`。

### 2. 出力の読み方

```markdown
### 指摘 #a3f10000 — 対象は指摘のあと移動しています（現在 37 行目）
場所: def render_with_file
比較: SB-2894 の未コミット変更

指摘時の行（34）:
```python
    button = get("user:upload")
```

会話:
- you: name の指定が追随していない
```

見出しの `—` の右が**対象の今の状態**。

| 表示 | 意味 | どうするか |
|------|------|-----------|
| 対象は指摘した時点のままです | まだ手が入っていない | そのまま直す |
| 対象は指摘のあと移動しています（現在 N 行目） | 行番号が動いただけ | N 行目を見る |
| 対象は指摘のあと書き換わっています | 誰かが（自分が）触った | 「同じ位置の現在の行」と見比べ、対応済みか判断する |
| 対象は消えています | 追えない | 引用の内容から対応済みか判断する。済んでいれば `resolve`、無関係に消えただけなら `drop` |
| 対象はコミットに取り込まれています | 未コミットの指摘がコミットされた | 内容を確認して対応済みなら `resolve` |
| ファイルが見つかりません | ファイルごと消えた | 同上 |

**場所は行番号ではなく「引用」と「場所」で特定する。** 行番号は編集で動く。
`指摘時の行` の逐語引用を検索して当てるのが確実。

### 3. 直す

指摘の内容に従ってコードを修正する。

### 4. 返信する

何をしたかを書く。人間はこれを GUI で読む。

```bash
kodama review reply a3f10000 --body "featuresMember:upload へ揃えました"
```

### 5. 解決にする

対応が済んだものだけ。判断が要るものは返信だけして残す。

```bash
kodama review resolve a3f10000
```

対象が消えていて対応の必要が無いと判断したときは取り下げる。

```bash
kodama review drop a3f10000
```

## 守ること

- **勝手に `resolve` しない。** 直したものだけ閉じる。判断が割れるものは
  `reply` で意見を書いて未解決のまま残す
- **`--author` を変えない。** 既定の `AI` のままにする。人間が誰の発言か
  見分けられなくなる
- **引用で場所を当てる。** 行番号だけを頼りにすると古い位置に飛ぶ
- **1 件ずつ返信する。** まとめて 1 つのコメントにすると、どの指摘への回答か
  分からなくなる

## 前提

`kodama` が PATH にあること。無ければ:

```bash
ln -sf /Applications/kodama.app/Contents/MacOS/kodama /usr/local/bin/kodama
```

GUI が起動していなくても動く。台帳を直接読み書きするため。
